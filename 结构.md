qq-farm-get-code/
├── index.html          ← Vite 入口
├── vite.config.js      ← Vite 配置
├── package.json        ← 依赖声明
├── package-lock.json
├── src/
│   ├── main.js         ← Vue 挂载
│   └── App.vue         ← 全部逻辑 + 样式
└── dist/               ← 构建产物（部署用）